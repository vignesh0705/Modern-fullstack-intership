const Loginpage=async()=>{
    await new Promise((resolve) => setTimeout(resolve, 2000));
    return(
        <div>
            <h1>Loginpage for seller</h1>
        </div>
    )
}

export default Loginpage;